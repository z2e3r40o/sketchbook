let sketch = function(p) {
    p.setup = function() {
        // add code here
    };

    p.draw = function() {
        // add code here
    };

    // add code here
};

new p5(sketch, window.document.getElementById('sketch'));
